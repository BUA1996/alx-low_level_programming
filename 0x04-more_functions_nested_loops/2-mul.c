#include "main.h"

/**
 * mul - entry point of the function to multiply two numbers
 * Return: always 0 or multiple
 *@a: number to multiply to b
 *@b: number to multiply with a
 */
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
